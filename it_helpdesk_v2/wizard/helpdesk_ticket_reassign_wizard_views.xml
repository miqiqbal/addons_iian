<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_helpdesk_ticket_reassign_wizard_form" model="ir.ui.view">
            <field name="name">helpdesk.ticket.reassign.wizard.form</field>
            <field name="model">helpdesk.ticket.reassign.wizard</field>
            <field name="arch" type="xml">
                <form string="Eskalasi / Reassign Ticket">
                    <group>
                        <field name="ticket_id" readonly="1"/>
                        <field name="current_engineer_id" readonly="1"/>
                        <field name="new_engineer_id" options="{'no_create': True}" placeholder="Pilih Engineer Penerima..."/>
                        <field name="reason" placeholder="Contoh: Eskalasi ke Spesialis Network / Server..."/>
                    </group>
                    <footer>
                        <button string="Konfirmasi Pengalihan" name="action_confirm_reassign" type="object" class="btn-primary"/>
                        <button string="Batal" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_helpdesk_ticket_reassign_wizard" model="ir.actions.act_window">
            <field name="name">Eskalasi / Reassign Ticket</field>
            <field name="res_model">helpdesk.ticket.reassign.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

    </data>
</odoo>
